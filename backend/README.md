# Hybrid Secret Scanner - Backend

This folder contains the core Machine Learning model for scanning secrets. 

## Setup
1. Define your environment via `.env` (refer to `.env.example`).
2. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
3. Run the model / engine:
   ```bash
   python main.py
   ```
